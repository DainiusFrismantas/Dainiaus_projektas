 /*function sendEmail(to, subject, body) {
    // This is a hypothetical function for sending an email
    // You would need to implement your own email sending logic here
    // using a server-side language or a third-party email service
    // Example: using Node.js and Nodemailer library
    /*
    const nodemailer = require("nodemailer");

    let transporter = nodemailer.createTransport({
      host: "smtp.example.com",
      port: 587,
      secure: false,
      auth: {
        user: "your-email@example.com",
        pass: "your-password"
      }
    });

    let mailOptions = {
      from: "your-email@example.com",
      to: to,
      subject: subject,
      text: body
    };

    transporter.sendMail(mailOptions, function(error, info) {
      if (error) {
        console.log(error);
      } else {
        console.log("Email sent: " + info.response);
      }
    });
    Šis kodas prideda įvykio klausytoją į kontaktų formos "submit" įvykį. Kai naudotojas paspaudžia mygtuką "Išsiųsti žinutę", vykdoma funkcija, kuri gauna įvestus duomenis, sukuria el. laiško kūną ir siunčia jį (hipotetiškai) naudojant sendEmail funkciją. Be to, šis kodas išvalo laukus ir rodo patvirtinimo pranešimą naudotojui.

  } */